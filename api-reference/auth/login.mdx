---
title: "Login"
api: "POST https://api.holysuck.com/v1/auth/login"
description: "Authenticate a user with email and password"
---

## Overview

The login endpoint allows users to authenticate with their email and password credentials. Upon successful authentication, you'll receive an access token that can be used for subsequent API requests.

## Request Body

<ParamField body="email" type="string" required>
  The user's email address
</ParamField>

<ParamField body="password" type="string" required>
  The user's password
</ParamField>

## Response

<ResponseField name="access_token" type="string" required>
  JWT access token for authenticating API requests
</ResponseField>

<ResponseField name="token_type" type="string" required>
  Token type, always "Bearer"
</ResponseField>

<ResponseField name="expires_in" type="number" required>
  Token expiration time in seconds (default: 3600)
</ResponseField>

<ResponseField name="refresh_token" type="string">
  Refresh token for obtaining new access tokens
</ResponseField>

## Example Request

<CodeGroup>

```bash cURL
curl -X POST https://api.holysuck.com/v1/auth/login \
  -H "Content-Type: application/json" \
  -d '{
    "email": "user@example.com",
    "password": "secure_password123"
  }'
```

```javascript JavaScript
const response = await fetch('https://api.holysuck.com/v1/auth/login', {
  method: 'POST',
  headers: {
    'Content-Type': 'application/json'
  },
  body: JSON.stringify({
    email: 'user@example.com',
    password: 'secure_password123'
  })
});

const authData = await response.json();
```

```python Python
import requests

response = requests.post(
    'https://api.holysuck.com/v1/auth/login',
    json={
        'email': 'user@example.com',
        'password': 'secure_password123'
    }
)

auth_data = response.json()
```

```go Go
package main

import (
    "bytes"
    "encoding/json"
    "fmt"
    "net/http"
)

func main() {
    loginData := map[string]string{
        "email":    "user@example.com",
        "password": "secure_password123",
    }
    
    jsonData, _ := json.Marshal(loginData)
    
    resp, err := http.Post(
        "https://api.holysuck.com/v1/auth/login",
        "application/json",
        bytes.NewBuffer(jsonData),
    )
    
    if err != nil {
        fmt.Println(err)
        return
    }
    defer resp.Body.Close()
}
```

</CodeGroup>

## Example Response

<CodeGroup>

```json 200 Success
{
  "access_token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",
  "token_type": "Bearer",
  "expires_in": 3600,
  "refresh_token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9..."
}
```

```json 401 Invalid Credentials
{
  "error": "invalid_credentials",
  "message": "The email or password provided is incorrect",
  "documentation_url": "https://docs.holysuck.com/errors/invalid_credentials"
}
```

```json 422 Validation Error
{
  "error": "validation_failed",
  "message": "The request contains invalid parameters",
  "details": [
    {
      "field": "email",
      "code": "invalid_format",
      "message": "Email must be a valid email address"
    }
  ],
  "documentation_url": "https://docs.holysuck.com/errors/validation_failed"
}
```

</CodeGroup>

## Using the Access Token

Once you receive the access token, include it in the `Authorization` header for subsequent API requests:

```http
Authorization: Bearer eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
```

## Security Considerations

<Warning>
**Password Security**: Passwords should be transmitted over HTTPS only. Never log or store passwords in plain text.
</Warning>

<Note>
**Token Storage**: Store access tokens securely on the client side. Consider using secure storage mechanisms like browser cookies with `httpOnly` flag or encrypted local storage.
</Note>

## Rate Limiting

The login endpoint is subject to special rate limiting to prevent brute force attacks:

- **5 attempts per minute** per IP address
- **10 attempts per hour** per email address
- **Lockout**: Account temporarily locked after 5 failed attempts

## Common Errors

| Status Code | Error Code | Description |
|-------------|------------|-------------|
| 401 | `invalid_credentials` | Email or password is incorrect |
| 422 | `validation_failed` | Request parameters are invalid |
| 429 | `rate_limit_exceeded` | Too many login attempts |
| 423 | `account_locked` | Account temporarily locked due to failed attempts |

## Next Steps

After successful login:

1. Store the access token securely
2. Use the token in the `Authorization` header for API requests
3. Implement token refresh logic before expiration
4. Consider implementing logout functionality